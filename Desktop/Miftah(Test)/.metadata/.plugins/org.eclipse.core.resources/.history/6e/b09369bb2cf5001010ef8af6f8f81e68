package Test9;

import java.time.Duration;
import org.openqa.selenium.By;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import io.github.bonigarcia.wdm.WebDriverManager;

public class TravelBookings {

    public static void main(String[] args) throws Exception {

        WebDriverManager.chromedriver().setup();
        WebDriver driver = new ChromeDriver();

        driver.get("https://dev.miftah.ai/");
        driver.manage().window().maximize();
        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(30));

        driver.findElement(By.id("email"))
              .sendKeys("jagadeeswara89@gmail.com");

        driver.findElement(By.id("password"))
              .sendKeys("Jaggu@89");

        driver.findElement(By.xpath("//button[text()='Login']")).click();

        Thread.sleep(5000);

        driver.findElement(By.xpath("//span[normalize-space()='Miftah Recommends']")).click();

        Thread.sleep(5000);
      driver.findElement(By.xpath("//button[.//span[normalize-space()='Travel']]")).click();
      Thread.sleep(5000);
      driver.findElement(By.xpath("//h3[normalize-space()='TRAvellllll']")).click();
      Thread.sleep(5000);
      driver.findElement(By.xpath("//button[normalize-space()='Book Now']")).click();
      Thread.sleep(5000);
   // Check-In
      driver.findElement(By.xpath("//label[text()='Check-In']/following::input[@type='date'][1]"))
            .sendKeys("20-01-2026");
      Thread.sleep(5000);
      driver.findElement(By.xpath("//label[text()='Check-In']/following::input[@type='time'][1]"))
            .sendKeys("10:00");
      Thread.sleep(5000);
      // Check-Out
      driver.findElement(By.xpath("//label[text()='Check-Out']/following::input[@type='date'][1]"))
            .sendKeys("22-01-2026");
      Thread.sleep(5000);
      driver.findElement(By.xpath("//label[text()='Check-Out']/following::input[@type='time'][1]"))
            .sendKeys("12:00");
      Thread.sleep(5000);
      WebDriverWait wait = new WebDriverWait(driver, Duration.ofSeconds(20));
   // Adults '+' button
      By adultsPlus = By.xpath("//div[label[contains(text(),'Adults')]]//*[name()='svg' and contains(@class,'lucide-plus')]");

      // Children '+' button
      By childrenPlus = By.xpath("//div[label[contains(text(),'Children')]]//*[name()='svg' and contains(@class,'lucide-plus')]");

      // Deluxe Room '+' button
      By deluxeRoomPlus = By.xpath("//div[contains(text(),'Deluxe')]/following-sibling::*//*[name()='svg' and contains(@class,'lucide-plus')]");
      
   // Click Adults '+' 5 times
      for (int i = 0; i < 5; i++) {
          wait.until(ExpectedConditions.elementToBeClickable(adultsPlus)).click();
          Thread.sleep(500);
      }

      // Click Children '+' 2 times
      for (int i = 0; i < 2; i++) {
          wait.until(ExpectedConditions.elementToBeClickable(childrenPlus)).click();
          Thread.sleep(500);
      }

      // Click Deluxe Room '+' 1 time
      for (int i = 0; i < 2; i++) {
          wait.until(ExpectedConditions.elementToBeClickable(deluxeRoomPlus)).click();
          Thread.sleep(500);
      }

      
      
      

   
      }
        
        
        
        
    }

